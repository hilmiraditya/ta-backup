apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "deploy-sample.fullname" . }}-test-connection"
  labels:
    {{- include "deploy-sample.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "deploy-sample.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
